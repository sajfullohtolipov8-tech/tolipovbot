version: '3.8'

services:
  bot:
    build: .
    restart: always
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
